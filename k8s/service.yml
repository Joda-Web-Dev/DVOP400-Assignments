apiVersion: v1
kind: Service
metadata:
  name: git-workflows-service
spec:
  selector:
    app: git-workflows-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer